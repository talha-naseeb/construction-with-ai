import Link from 'next/link';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function RecordDetail({ eyebrow, title, backHref, backLabel, rows, note, related = [] }: { eyebrow: string; title: string; backHref: string; backLabel: string; rows: Array<[string, string]>; note: string; related?: Array<{ label: string; value: string; href?: string }> }) {
  return <div className="space-y-5"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-muted-foreground">{eyebrow}</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{title}</h1></div><Button asChild variant="outline"><Link href={backHref}>← Back to {backLabel}</Link></Button></header><section className="grid gap-4 lg:grid-cols-[1.3fr_.7fr]"><Card><CardHeader><CardTitle>Record details</CardTitle></CardHeader><CardContent><dl className="grid gap-y-4 sm:grid-cols-[150px_1fr]">{rows.map(([label, value]) => <><dt key={`${label}-term`} className="text-xs font-semibold uppercase tracking-wide text-muted-foreground">{label}</dt><dd key={`${label}-value`} className="text-sm font-medium">{value}</dd></>)}</dl></CardContent></Card><Card><CardHeader><p className="text-[10px] font-extrabold uppercase tracking-[.13em] text-muted-foreground">Workflow note</p><CardTitle>Backend action required</CardTitle></CardHeader><CardContent className="space-y-4 text-sm leading-6 text-muted-foreground"><p>{note}</p><div className="rounded-md border border-amber-300 bg-amber-50 p-3 text-xs text-amber-950">This local demo view does not apply business-state changes or reveal protected data.</div></CardContent></Card></section>{related.length > 0 && <Card><CardHeader><p className="text-[10px] font-extrabold uppercase tracking-[.13em] text-muted-foreground">Related history</p><CardTitle>Customer activity</CardTitle></CardHeader><CardContent className="divide-y">{related.map((item) => <div key={`${item.label}-${item.value}`} className="flex items-center justify-between gap-4 py-3 text-sm"><div><p className="font-semibold">{item.label}</p><p className="mt-1 text-muted-foreground">{item.value}</p></div>{item.href && <Button asChild size="sm" variant="ghost"><Link href={item.href}>Open</Link></Button>}</div>)}</CardContent></Card>}</div>;
}
