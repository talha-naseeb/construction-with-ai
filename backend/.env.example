# Copy required values from the Vercel Neon integration into your local environment.
# Use the pooled URL at runtime and the unpooled URL for Drizzle migrations.
DATABASE_URL=
DATABASE_URL_UNPOOLED=
PORT=3001
