import Link from 'next/link';
import { notFound } from 'next/navigation';
import { LeadWorkflow } from '@/components/admin/lead-workflow';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { operationsRepository } from '@/lib/operations-repository';

export default async function LeadDetail({ params }: { params: Promise<{ leadId: string }> }) { const lead = operationsRepository.getLead((await params).leadId); if (!lead) notFound(); return <div className="space-y-5"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-[10px] font-extrabold uppercase tracking-[.15em] text-muted-foreground">Lead · {lead.id}</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{lead.customer}</h1></div><Button asChild variant="outline"><Link href="/leads">← Back to leads</Link></Button></header><section className="grid gap-4 xl:grid-cols-[1.2fr_.8fr]"><Card><CardHeader><CardTitle>Request details</CardTitle></CardHeader><CardContent><dl className="grid gap-y-4 text-sm sm:grid-cols-[150px_1fr]"><dt className="text-muted-foreground">Requested service</dt><dd className="font-medium">{lead.service}</dd><dt className="text-muted-foreground">Location</dt><dd className="font-medium">{lead.location}</dd><dt className="text-muted-foreground">Source</dt><dd className="font-medium">{lead.source}{lead.sourceCallId && <> · <Link className="text-ops-success underline" href={`/calls/${lead.sourceCallId}`}>View call</Link></>}</dd><dt className="text-muted-foreground">Owner</dt><dd className="font-medium">{lead.owner}</dd><dt className="text-muted-foreground">Follow-up</dt><dd className="font-medium">{lead.followUp}</dd></dl></CardContent></Card><LeadWorkflow lead={lead}/></section></div>; }
