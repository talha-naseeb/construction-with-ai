'use client';
import { useState } from 'react';
import type { JobRecord, JobStatus } from '@/lib/operations-repository';
import { Button } from '@/components/ui/button';
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
const nextStatus: Record<JobStatus, JobStatus | null> = { 'Site visit': 'Quote pending', 'Quote pending': 'Scheduled', Scheduled: 'In progress', 'In progress': 'Completed', Completed: null, Cancelled: null };
export function JobWorkflow({ job }: { job: JobRecord }) { const [status, setStatus] = useState(job.status); const [message, setMessage] = useState(''); const [notes, setNotes] = useState(''); const next = nextStatus[status]; const closed = status === 'Completed' || status === 'Cancelled'; return <Card><CardHeader><p className="text-[10px] font-extrabold uppercase tracking-[.13em] text-muted-foreground">Job status</p><CardTitle>{status}</CardTitle></CardHeader><CardContent className="space-y-3"><p className="text-sm leading-6 text-muted-foreground">Only the next valid transition is available in this local demo.</p>{next ? <Button className="w-full" onClick={() => { setStatus(next); setMessage(`Moved to ${next} in this local demo.`); }}>Move to {next}</Button> : <p className="rounded-md border border-amber-300 bg-amber-50 p-3 text-xs text-amber-950">This job is closed. Reopening requires an authorised backend action.</p>}<label className="grid gap-2 text-xs font-semibold">Work note<textarea className="min-h-20 rounded-md border border-input bg-transparent p-3 text-sm" value={notes} onChange={(event) => setNotes(event.target.value)} placeholder="Add a site note or completion summary" disabled={closed}/></label>{!closed && <Button variant="outline" className="w-full" onClick={() => setMessage(notes.trim() ? 'Work note saved in this local demo.' : 'Add a work note before saving.')}>Save work note</Button>}{!closed && <Button variant="ghost" className="w-full text-destructive hover:text-destructive" onClick={() => { setStatus('Cancelled'); setMessage('Job cancelled in this local demo.'); }}>Cancel job</Button>}{message && <p className="rounded-md bg-muted p-3 text-xs text-muted-foreground">{message}</p>}</CardContent></Card>; }
